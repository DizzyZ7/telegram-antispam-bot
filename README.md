# 🛡️ telegram-antispam-bot

**telegram-antispam-bot** — это специализированный шлюз безопасности, работающий по принципу **Challenge-Response**. Бот предназначен для жесткой фильтрации входящих субъектов и предотвращения спам-атак на закрытые периметры (чаты).

Также бот поддерживает **дневную аналитику чата**: сбор текстовых сообщений за текущие сутки, ручную сводку по команде и автоматическую отправку итогов дня.

## 🧩 Механика защиты

Система работает в режиме **Zero Trust** (Нулевое доверие):
1.  **Intercept**: Захват события входа нового участника.
2.  **Quarantine**: Мгновенное лишение прав на отправку любого контента.
3.  **Challenge**: Генерация уникальной арифметической задачи с ограничением по времени (TTL: 60 сек).
4.  **Verification**: 
    *   При успехе: Полная авторизация и возврат прав.
    *   При провале/тайм-ауте: Немедленная терминация сессии (Ban/Kick).

## ⚙️ Конфигурация

Система требует наличия следующих переменных среды:
*   `BOT_TOKEN` — уникальный ключ доступа к API Telegram.
*   `ALLOWED_CHATS` — белый список ID чатов через запятую (например: `-1001,-1002`).
*   `SUMMARY_STORAGE_PATH` — путь к SQLite базе для временного хранения сообщений (по умолчанию: `daily_summary.db`).
*   `SUMMARY_DEFAULT_TIME` — время авто-сводки в формате `HH:MM` (по умолчанию: `23:59`).
*   `SUMMARY_TIMEZONE` — таймзона для расчета суток и авто-сводки (по умолчанию: `Europe/Moscow`).
*   `SUMMARY_MIN_MESSAGES` — минимальное число сообщений для полноценной аналитики (по умолчанию: `12`).

## 🧠 Дневная аналитика

Поддерживаемые команды:
* `/summary` или `/today` — полная сводка за текущие сутки.
* `/stats` — краткая статистика за текущие сутки без расширенной аналитики.
* `/reset_stats` — очистка статистики текущего чата (только для админов).
* `/summary_settings` — показать настройки авто-сводки.
* `/summary_settings HH:MM` — установить время авто-сводки (только для админов).
* `/summary_settings on|off` — включить/выключить авто-сводку (только для админов).
* `/captcha_stats` — статистика антиспам-капчи.

Правила хранения:
* Сохраняются только текстовые сообщения из групп/супергрупп.
* Данные разделяются по `chat_id`.
* Сообщения автоматически удаляются по TTL (старше 24 часов).
* После автоматической отправки дневной сводки сообщения чата очищаются.

## 🛠 Установка и запуск

```bash
# Клонирование репозитория
git clone https://github.com

# Установка зависимостей
pip install -r requirements.txt

# Запуск системы
python main.py
